import React from 'react';

const Upload = () => {
  return (
    <div style={styles.container}>
      <h1>Upload Page</h1>
      <p>This is where users can upload content.</p>
    </div>
  );
};

const styles = {
  container: {
    padding: '2rem',
    textAlign: 'center',
  },
};

export default Upload;
